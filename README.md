# Wallet Monitoring App

This is a Streamlit application for monitoring wallet transactions through bloXroute.


```bash
python3 -m venv .venv
source .venv/bin/activate

pip install -r requirements.txt
streamlit run main.py
